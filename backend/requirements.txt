# Serveur
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9

# Séparation voix / bruit via modèles UVR (Kim Vocal 2, RoFormer, MDX…)
# Utilise onnxruntime ; télécharge les modèles à la demande.
#   - CPU / Mac CoreML : audio-separator[cpu] (embarque onnxruntime)
#   - GPU NVIDIA        : remplace par  audio-separator[gpu]
audio-separator[cpu]>=0.24
soundfile>=0.12
numpy>=1.24

# Fournit un binaire ffmpeg (requis par audio-separator). install.ps1 le copie
# dans backend/bin/ffmpeg.exe pour l'embarquer avec le moteur.
imageio-ffmpeg>=0.5
